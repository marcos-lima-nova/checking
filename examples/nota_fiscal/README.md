# Exemplos: nota_fiscal

Coloque aqui páginas/documentos de exemplo do tipo **nota_fiscal**
(imagens, PDFs ou artefatos de OCR) usados como referência para ajustar as
regras em `config/document_rules.yaml`.
